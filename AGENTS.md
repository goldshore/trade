# Agent Instructions

This repository is for a FRONTEND-ONLY trading UI.

## API Usage

- This repository must **NEVER** define or deploy a Worker, Pages project, or service named "goldshore-api".
- The only API it should call is at `https://api.goldshore.org`.

Any changes that violate these rules will be rejected.

# @goldshore/db

SQL schema and future Drizzle ORM helpers for the GoldShore D1 database. Apply `schema.sql` to provision the baseline tables for blog posts and storefront products, then extend with migrations as needed.

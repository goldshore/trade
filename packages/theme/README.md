# @goldshore/theme

Shared CSS tokens and lightweight components for GoldShore surfaces. The initial implementation lives in `apps/web/src/styles/theme.css` and can be modularized into this package as the design system evolves.
